diff --git a/static/sync_room.html b/static/sync_room.html
index f1a60f5..15761e3 100644
--- a/static/sync_room.html
+++ b/static/sync_room.html
@@ -179,10 +179,12 @@
               </div>
             </div>
 
-            <div id="aiCoachContainer" style="display:none;margin-top:1.5rem;padding-top:1rem;border-top:1px solid var(--border);">
-              <div style="font-family:var(--font-m);font-size:0.55rem;letter-spacing:0.15em;text-transform:uppercase;color:var(--amber);font-weight:600;margin-bottom:0.8rem;">
-                <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="var(--amber)" stroke-width="2" style="display:inline;vertical-align:middle;"><path d="M12 2a10 10 0 1010 10"/><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
-                <span data-lang="en"> AI Conversation Coach</span><span data-lang="hi" style="display:none;"> AI Conversation Coach</span>
+            <div id="aiCoachContainer" style="display:none;margin-top:1.5rem;padding-top:1.2rem;border-top:1px solid var(--border);">
+              <div style="display:flex;align-items:center;gap:8px;margin-bottom:1rem;">
+                <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="var(--amber)" stroke-width="2" style="flex-shrink:0;"><path d="M12 2a10 10 0 1010 10"/><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
+                <span style="font-family:var(--font-b);font-size:1.05rem;font-weight:700;color:var(--ivory);letter-spacing:-0.01em;">
+                  <span data-lang="en">AI Conversation Coach</span><span data-lang="hi" style="display:none;">AI Conversation Coach</span>
+                </span>
               </div>
               <div id="aiCoachList"></div>
             </div>
@@ -988,9 +990,9 @@ sessionPromise.then(function(s) {
     var analysis = session.analysis;
     analysisData = analysis;
 
-    var cardStyle = 'background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:1rem;margin-bottom:0.8rem;';
-    var labelStyle = 'font-family:var(--font-m);font-size:0.5rem;letter-spacing:0.1em;text-transform:uppercase;color:var(--ivory-faint);margin-bottom:2px;';
-    var textStyle = 'font-family:var(--font-rc);font-size:0.82rem;color:var(--ivory);line-height:1.5;';
+    var cardStyle = 'background:var(--surface);border:1px solid var(--border-lit);border-radius:12px;padding:1.1rem 1.2rem;margin-bottom:0.8rem;box-shadow:var(--shadow-sm);';
+    var labelStyle = 'font-family:var(--font-m);font-weight:700;font-size:0.64rem;letter-spacing:0.09em;text-transform:uppercase;margin-bottom:5px;';
+    var textStyle = 'font-family:var(--font-rc);font-size:0.9rem;color:var(--ivory);line-height:1.6;font-weight:500;';
 
     // ── Personalize Step 1 "Establish Psychological Safety" using AI analysis ──
     var ps = analysis.psychological_safety;
@@ -1044,11 +1046,27 @@ sessionPromise.then(function(s) {
       if (coachContainer && coachList) {
         coachList.innerHTML = '';
         analysis.conversation_coach.forEach(function(cc) {
-          var html = '<div style="' + cardStyle + 'border-left:3px solid var(--amber);">';
-          if (cc.immediate_response) { html += '<div style="margin-bottom:6px;"><div style="' + labelStyle + '">Immediate Response</div><div style="' + textStyle + '">' + escHtml(cc.immediate_response) + '</div></div>'; }
-          if (cc.better_follow_up_question) { html += '<div style="margin-bottom:6px;"><div style="' + labelStyle + '">Better Follow-up Question</div><div style="' + textStyle + '">' + escHtml(cc.better_follow_up_question) + '</div></div>'; }
-          if (cc.avoid_saying) { html += '<div style="margin-bottom:6px;"><div style="' + labelStyle + 'color:var(--red);">Avoid Saying</div><div style="' + textStyle + 'color:var(--red-soft);">' + escHtml(cc.avoid_saying) + '</div></div>'; }
-          if (cc.why_it_works) { html += '<div style="margin-bottom:6px;"><div style="' + labelStyle + '">Why This Works</div><div style="' + textStyle + 'color:var(--ivory-dim);">' + escHtml(cc.why_it_works) + '</div></div>'; }
+          var html = '<div style="' + cardStyle + '">';
+          if (cc.immediate_response) {
+            html += '<div style="margin-bottom:12px;padding:0.7rem 0.85rem;background:var(--amber-soft);border-left:3px solid var(--amber);border-radius:6px;">' +
+              '<div style="' + labelStyle + 'color:var(--amber);">Immediate Response</div>' +
+              '<div style="' + textStyle + '">' + escHtml(cc.immediate_response) + '</div></div>';
+          }
+          if (cc.better_follow_up_question) {
+            html += '<div style="margin-bottom:12px;padding:0.7rem 0.85rem;background:var(--teal-soft);border-left:3px solid var(--teal);border-radius:6px;">' +
+              '<div style="' + labelStyle + 'color:var(--teal);">Better Follow-up Question</div>' +
+              '<div style="' + textStyle + '">' + escHtml(cc.better_follow_up_question) + '</div></div>';
+          }
+          if (cc.avoid_saying) {
+            html += '<div style="margin-bottom:12px;padding:0.7rem 0.85rem;background:var(--red-soft);border-left:3px solid var(--red);border-radius:6px;">' +
+              '<div style="' + labelStyle + 'color:var(--red);">&#10005; Avoid Saying</div>' +
+              '<div style="' + textStyle + '">' + escHtml(cc.avoid_saying) + '</div></div>';
+          }
+          if (cc.why_it_works) {
+            html += '<div style="padding-top:0.65rem;border-top:1px dashed var(--border-lit);">' +
+              '<div style="' + labelStyle + 'color:var(--ivory-dim);">Why This Works</div>' +
+              '<div style="font-family:var(--font-rc);font-size:0.84rem;color:var(--ivory-dim);line-height:1.55;font-style:italic;">' + escHtml(cc.why_it_works) + '</div></div>';
+          }
           html += '</div>';
           coachList.innerHTML += html;
         });
