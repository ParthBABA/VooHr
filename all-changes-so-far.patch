diff --git a/static/conversation-workspace.html b/static/conversation-workspace.html
index 86cfa17..7f80d48 100644
--- a/static/conversation-workspace.html
+++ b/static/conversation-workspace.html
@@ -336,15 +336,21 @@ window.voohrInitWorkspace = function() {
       container.innerHTML = '<div class="ws-empty">' + (state.lines.length ? 'No lines match your filters.' : 'No transcript available for this session.') + '</div>';
       return;
     }
+    var empName = (state.employee && state.employee.name) ? state.employee.name.trim() : '';
+    var empPhoto = state.employee && state.employee.photo;
+    var empInitials = empName ? empName.split(/\s+/).map(function(p) { return p.charAt(0); }).slice(0, 2).join('').toUpperCase() : 'EE';
     var html = visible.map(function(l) {
       var pinned = !!state.pinned[l.idx];
-      var initials = l.speaker === 'HR' ? 'HR' : 'EE';
+      var isHr = l.speaker === 'HR';
+      var avatarInner = isHr
+        ? 'HR'
+        : (empPhoto ? '<img src="' + esc(empPhoto) + '" alt="' + esc(empName || 'Employee') + '">' : esc(empInitials));
       return (
         '<div class="ws-line' + (pinned ? ' pinned' : '') + '" data-idx="' + l.idx + '">' +
-          '<div class="ws-line-avatar ' + (l.speaker === 'HR' ? 'hr' : 'emp') + '">' + initials + '</div>' +
+          '<div class="ws-line-avatar ' + (isHr ? 'hr' : 'emp') + (!isHr && empPhoto ? ' has-photo' : '') + '">' + avatarInner + '</div>' +
           '<div class="ws-line-body">' +
             '<div class="ws-line-head">' +
-              '<span class="ws-line-speaker ' + (l.speaker === 'HR' ? 'hr' : 'emp') + '">' + l.speaker + '</span>' +
+              '<span class="ws-line-speaker ' + (isHr ? 'hr' : 'emp') + '">' + (isHr ? 'HR' : esc(empName || 'Employee')) + '</span>' +
               (state.showTime ? '<span class="ws-line-time">' + fmtTime(l.time) + '</span>' : '') +
             '</div>' +
             '<div class="ws-line-text">' + highlight(l.text) + '</div>' +
diff --git a/static/login.html b/static/login.html
index 16c2779..cd16be4 100644
--- a/static/login.html
+++ b/static/login.html
@@ -3382,7 +3382,7 @@ window.__splineReady=(function(){var p={};p.promise=new Promise(function(r,j){p.
 <script id="voovr-critical-navigation">
 (function(){
  var o=document.getElementById('voovr-page-transition'),seen=new Set(),ht;
- var LEGAL_ASSET='Banding - Copy@1-1536x730.png';
+ var LEGAL_ASSET='Banding - Copy@1-1536x730.webp';
  var LEGAL_TARGETS={'/privacy-policy.html':1,'/terms-of-service.html':1};
  var legalCache=null;
  function u(a){
diff --git a/static/privacy-policy.html b/static/privacy-policy.html
index e35bee8..9e0088b 100644
--- a/static/privacy-policy.html
+++ b/static/privacy-policy.html
@@ -10,7 +10,7 @@
   </style>
 </head>
 <body style="background:var(--void); color:var(--ivory); font-family:var(--font-b); font-size:15px; line-height:1.6; min-height:100vh; overflow-x:hidden; padding:40px 20px; box-sizing:border-box;">
-  <img class="bg-banding" src="Banding - Copy@1-1536x730.png" alt="" aria-hidden="true" width="1536" height="730">
+  <img class="bg-banding" src="Banding - Copy@1-1536x730.webp" alt="" aria-hidden="true" width="1536" height="730">
   <div style="max-width:800px;margin:0 auto;position:relative;z-index:10;background:var(--ink);border:1px solid var(--border);border-radius:12px;padding:40px 32px;box-shadow:var(--shadow-md);">
     <div style="margin-bottom:24px;">
       <a href="/login2.html" style="font-family:var(--font-lato); font-size:0.7rem; color:var(--amber); letter-spacing:0.05em; text-transform:uppercase; text-decoration:none;">← Back to Login</a>
diff --git a/static/settings.html b/static/settings.html
index 81f3257..009baab 100644
--- a/static/settings.html
+++ b/static/settings.html
@@ -654,16 +654,23 @@ function clearFieldError(input){
 
   function deviceLabel(d){
     if (!d) return 'Unknown device';
+    // Show a simple, friendly device name (Laptop/Phone/Tablet) rather than
+    // the raw browser/OS string. Falls back to the detailed browser+OS combo
+    // for device types we don't have a friendly name for.
+    if (d.device_type === 'Desktop') return 'Laptop';
+    if (d.device_type === 'Mobile') return 'Phone';
+    if (d.device_type === 'Tablet') return 'Tablet';
     var parts = [];
-    // Treat absent/unparsed pieces ("Unknown") as missing so a session with
-    // no usable device metadata falls back cleanly instead of rendering
-    // the literal string "Unknown on Unknown".
     if (d.browser && d.browser !== 'Unknown') parts.push(d.browser);
     if (d.os && d.os !== 'Unknown') parts.push(d.os);
     return parts.length ? parts.join(' on ') : 'Unknown device';
   }
 
   function locationLabel(loc){
+    // Real per-IP geo lookup (see login_flow.py:_lookup_location). This only
+    // shows "Local network" when the session's IP genuinely couldn't be
+    // geolocated (private/local IP, or the lookup failed) — it is never a
+    // hardcoded value, so each session reflects its own actual city/region.
     if (!loc) return 'Local network';
     var parts = [];
     if (loc.city) parts.push(loc.city);
diff --git a/static/style.css b/static/style.css
index e4a8653..3610472 100644
--- a/static/style.css
+++ b/static/style.css
@@ -125,14 +125,14 @@ h1,h2,h3,h4,h5,h6{font-family:var(--font-d);font-weight:600;text-transform:upper
 .sidebar.collapsed .nav-item{padding:0.7rem 0;gap:0;}
 .sidebar.collapsed .nav-item svg{margin:0;}
 .sidebar.collapsed .sidebar-footer .user-info{opacity:0;width:0;overflow:hidden;transition:opacity 0.25s ease;}
-.sidebar.collapsed .sidebar-footer{justify-content:center;padding:1.2rem 0;}
+.sidebar.collapsed .sidebar-footer{justify-content:center;padding:1.2rem 0;gap:0;}
 .sidebar.icon-only{width:80px;transition:width 0.35s cubic-bezier(0.4,0,0.2,1);}
 .sidebar.icon-only .nav-section{opacity:0;height:0;padding:0;overflow:hidden;transition:opacity 0.25s ease,height 0.25s ease;}
 .sidebar.icon-only .sidebar-logo{font-size:0;gap:0;justify-content:center;padding:1.8rem 1.5rem;border-bottom:1px solid var(--border);transition:font-size 0.3s ease;}
 .sidebar.icon-only .sidebar-logo .logo-mark{margin:0 auto;width:28px;height:28px;background:linear-gradient(135deg,var(--violet),var(--amber));display:flex;align-items:center;justify-content:center;font-size:0.7rem;color:var(--void);font-weight:800;border-radius:6px;}
 .sidebar.icon-only .sidebar-logo-img{height:26px;margin:0 auto;display:block;object-fit:contain;}
 .sidebar.icon-only .sidebar-footer .user-info{opacity:0;width:0;overflow:hidden;transition:opacity 0.25s ease;}
-.sidebar.icon-only .sidebar-footer{justify-content:center;padding:1.2rem 0;border-top:1px solid var(--border);}
+.sidebar.icon-only .sidebar-footer{justify-content:center;padding:1.2rem 0;border-top:1px solid var(--border);gap:0;}
 .sidebar.icon-only ~ .main-content{margin-left:80px;transition:margin-left 0.35s cubic-bezier(0.4,0,0.2,1);}
 .sidebar-header{padding:1.8rem 1.5rem;border-bottom:1px solid var(--border);}
 .sidebar.icon-only .nav-item{font-size:0;white-space:nowrap;padding:0.7rem 0;gap:0;transition:font-size 0.3s ease;}
@@ -730,7 +730,7 @@ body.lang-hi [data-lang-block="en"]{display:none;}
 .ws-gate-body{font-size:0.9rem;color:var(--ivory-dim);line-height:1.7;margin-bottom:1.8rem;}
 .ws-gate-note{margin-top:1.2rem;font-family:var(--font-m);font-size:0.7rem;letter-spacing:0.1em;color:var(--ivory-faint);}
 
-.ws-grid{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(0,1fr);gap:1.2rem;align-items:stretch;}
+.ws-grid{display:grid;grid-template-columns:minmax(0,3fr) minmax(0,7fr);gap:1.2rem;align-items:stretch;}
 .ws-transcript{display:flex;flex-direction:column;gap:1rem;height:100%;}
 .ws-coach{display:flex;flex-direction:column;gap:1.2rem;}
 .ws-toolbar{display:flex;align-items:center;gap:0.6rem;padding:0.9rem 1.2rem;border-bottom:1px solid var(--border);flex-wrap:wrap;}
@@ -769,6 +769,8 @@ body.lang-hi [data-lang-block="en"]{display:none;}
 .ws-line-avatar{flex-shrink:0;width:30px;height:30px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-family:var(--font-m);font-size:0.58rem;font-weight:700;letter-spacing:0.04em;margin-top:1px;}
 .ws-line-avatar.hr{background:var(--violet-soft);border:1px solid rgba(123,79,255,0.35);color:var(--violet);}
 .ws-line-avatar.emp{background:var(--teal-soft);border:1px solid rgba(51,160,143,0.35);color:var(--teal);}
+.ws-line-avatar.has-photo{padding:0;overflow:hidden;}
+.ws-line-avatar.has-photo img{width:100%;height:100%;border-radius:50%;object-fit:cover;display:block;}
 .ws-line-body{flex:1;min-width:0;}
 .ws-line-head{display:flex;align-items:center;gap:0.6rem;margin-bottom:0.15rem;}
 .ws-line-speaker{font-family:var(--font-m);font-size:0.58rem;font-weight:700;letter-spacing:0.14em;text-transform:uppercase;}
diff --git a/static/terms-of-service.html b/static/terms-of-service.html
index 674d695..f5ea1f8 100644
--- a/static/terms-of-service.html
+++ b/static/terms-of-service.html
@@ -10,7 +10,7 @@
   </style>
 </head>
 <body style="background:var(--void); color:var(--ivory); font-family:var(--font-b); font-size:15px; line-height:1.6; min-height:100vh; overflow-x:hidden; padding:40px 20px; box-sizing:border-box;">
-  <img class="bg-banding" src="Banding - Copy@1-1536x730.png" alt="" aria-hidden="true" width="1536" height="730">
+  <img class="bg-banding" src="Banding - Copy@1-1536x730.webp" alt="" aria-hidden="true" width="1536" height="730">
   <div style="max-width:800px;margin:0 auto;position:relative;z-index:10;background:var(--ink);border:1px solid var(--border);border-radius:12px;padding:40px 32px;box-shadow:var(--shadow-md);">
     <div style="margin-bottom:24px;">
       <a href="/login2.html" style="font-family:var(--font-lato); font-size:0.7rem; color:var(--amber); letter-spacing:0.05em; text-transform:uppercase; text-decoration:none;">← Back to Login</a>
diff --git a/static/verify-totp-gate.html b/static/verify-totp-gate.html
index adcf137..cf0b0a0 100644
--- a/static/verify-totp-gate.html
+++ b/static/verify-totp-gate.html
@@ -13,13 +13,13 @@
 /* ── TOTP background artwork (scoped) ──
    Layered onto the existing body background mechanism (same
    background-attachment:fixed pattern as style.css): a faint ink veil for
-   text contrast, the local totp-background.png artwork, then the original
+   text contrast, the local totp-background.webp artwork, then the original
    radial gradients. Purely decorative layer — no layout impact, sits
    behind the card (which keeps its solid var(--ink) background + z-index). */
 body{
   background-image:
     linear-gradient(rgba(8,10,15,0.32), rgba(8,10,15,0.32)),
-    url("/totp-background.png"),
+    url("/totp-background.webp"),
     radial-gradient(ellipse 80% 60% at 50% 0%, rgba(80,120,200,0.04) 0%, transparent 70%),
     radial-gradient(ellipse 60% 50% at 80% 100%, rgba(212,136,74,0.02) 0%, transparent 60%);
   background-size:cover, cover, auto, auto;
diff --git a/static/verify-totp-login.html b/static/verify-totp-login.html
index e708f62..7ea8372 100644
--- a/static/verify-totp-login.html
+++ b/static/verify-totp-login.html
@@ -12,13 +12,13 @@
 /* ── TOTP background artwork (scoped) ──
    Layered onto the existing body background mechanism (same
    background-attachment:fixed pattern as style.css): a faint ink veil for
-   text contrast, the local totp-background.png artwork, then the original
+   text contrast, the local totp-background.webp artwork, then the original
    radial gradients. Purely decorative layer — no layout impact, sits
    behind the card (which keeps its solid var(--ink) background + z-index). */
 body{
   background-image:
     linear-gradient(rgba(8,10,15,0.32), rgba(8,10,15,0.32)),
-    url("/totp-background.png"),
+    url("/totp-background.webp"),
     radial-gradient(ellipse 80% 60% at 50% 0%, rgba(80,120,200,0.04) 0%, transparent 70%),
     radial-gradient(ellipse 60% 50% at 80% 100%, rgba(212,136,74,0.02) 0%, transparent 60%);
   background-size:cover, cover, auto, auto;
